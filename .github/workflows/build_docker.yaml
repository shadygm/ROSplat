name: Test ROSplat Docker Build

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  test-build:
    runs-on: ubuntu-latest

    steps:
      - name: Make script executable
        run: chmod +x ./docker/run_docker.sh

      - name: Run Docker build script with no cache
        run: ./docker/run_docker.sh -n
